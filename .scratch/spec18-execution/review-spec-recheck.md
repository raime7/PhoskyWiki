# Spec 复审

固定范围：`git diff 855285b5be39d1dc8aafeef31a6c0a77d1e08905...ab83ca4eb8b4c5a3184c01f895bd394872e11b99`（80 个文件，非空）；已核对对应提交列表。沿用初审对核心实现的检查，重点复核 `8fc4da0` 缓存补丁与 `ab83ca4` 别名补丁。原报告 `review-spec.md` 保留。

- **原 P2 别名边界丢失：resolved。** Spec B：“创建、直编、受理、修订 diff 与回滚均能表达相同的元数据快照。”`src/lib/alias-input.ts:2–39` 通过共享格式化/解析保留顿号、逗号、引号、反斜杠和控制字符；`src/components/submission-form.tsx:84,133,164` 将新建、元数据编辑、重提与草稿接到同一规则，旧版未修改预填也恢复原数组。`alias-roundtrip.spec.ts` 通过浏览器和提交/历史 HTTP 覆盖仅改简介、刷新草稿及两种重提；不再发生原先的 `['甲、乙']` → `['甲','乙']`。

- **原 P2 图谱缓存不一致：resolved。** Spec D：“内容读取、双链目标、来源反链和引用统计、图谱、推荐与搜索同步使用这一口径”；Spec E：“对依赖这些关系的派生缓存或索引按现有机制更新”。site/local 响应现均为 `no-store`，local 的 400/404 同样禁止存储（`src/app/api/graph/site/route.ts:9`、`local/route.ts:7`）。真实浏览器默认 `fetch` 使用相同 URL 的回归覆盖删除/恢复和 CLI 校对，并有旧实现失败、新实现通过记录；不靠缓存绕过参数掩盖问题。无需扩展为主动推送更新已显示画布。

补丁未发现新增 Spec 缺口或独立范围扩张。图谱操作说明已更新；明确允许的搜索 total/facets 派生索引延迟不重新列为问题。

**结论：原 2 项均 resolved；unresolved 0；新增发现 0。** 本次为只读源码、测试和证据复审，未运行修改数据库的测试；最终冻结提交的整体验收由主任务独立执行，本结论不替代尚在运行的验收结果。
