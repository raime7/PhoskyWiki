# Spec

审查范围：`git diff 855285b5be39d1dc8aafeef31a6c0a77d1e08905...a935c6243c75bd6ed006d4be957b6720df2bad1b`；另确认 `e5907c0` 仅删除测试文件末尾空行。

- **[P2] 词条信息编辑会拆坏既有别名** — `src/components/submission-form.tsx:158`（预填见 80 行，新建见 167 行）。Spec B：“创建、直编、受理、修订 diff 与回滚均能表达相同的元数据快照。”新建表单只按逗号/换行拆分，因此输入 `甲、乙` 保存的是一个合法别名；编辑表单却新增顿号分隔。以后仅修改简介、甚至原样提交，别名也变成 `['甲','乙']`，直编或受理后永久改写原字段。只读执行这两段真实转换已确认该往返差异。应采用能保留现有数组元素边界的编辑方式，并覆盖新建→仅改简介→别名不变。

- **[P2] 删除和存量校对没有使图谱 HTTP 缓存失效** — `src/lib/history.ts:79–82`、`scripts/reconcile-links.ts:71–72`。Spec D：“内容读取、双链目标、来源反链和引用统计、图谱、推荐与搜索同步使用这一口径”；Spec E：“对依赖这些关系的派生缓存或索引按现有机制更新”。两条写路径只同步搜索；图谱端点仍返回 `public, max-age=60, stale-while-revalidate=300`，局部图谱 fetch 也使用默认缓存。读者先加载两跳图谱，再删除父页或运行校对，切换跳数可继续拿到旧节点/旧边，与已更新反链、推荐不一致，删除词条时旧节点还会进入 404。应调整缓存策略或提供写后失效；现有测试的 `page.request` 直接请求不能证明真实浏览器缓存场景。

未发现独立的范围扩张。其余核心路径与约束已核对。搜索旧索引的 total/facets 暂态未另列缺陷：ADR-0002 保留异步派生索引及校对机制，公开命中本身已按 PG 当前可见性过滤。最终服务契约的 R2 环境跳过已如实披露，不计作通过。

合计 2 项，最高 P2。仅只读审查与别名纯转换验证；未运行会修改数据库的测试。
