{
    admin off
}

:8080 {
    reverse_proxy app:3000
}

phoskywiki.org {
    tls {
        issuer acme {
            disable_tlsalpn_challenge
        }
    }
    # D02 enforces invitations in the application, including direct signup APIs.
    reverse_proxy app:3000
}
