# D05 实施与验收记录

日期：2026-09-10。任务：[D05 #39](https://github.com/raime7/PhoskyWiki/issues/39)。代码与本记录在同一提交中；起始代码为 `ede4c192774715ddde44718437e05ae7d2c237e5`，工作期间其他任务另有 D02/D03 提交，本次只提交 D05 文件。

实现 PostgreSQL 持久化账号频率计数、事务性上传容量/待完成数量预留、过期完成拒绝、只清理暂存对象的运维命令与脱敏用量报告。配置、并发/窗口语义、拒绝响应和费用边界见 [运维说明](../write-and-upload-limits.md)。现有认证、冻结、双人受理、永久图片及历史修订引用规则保持。

## 已执行

| 验证 | 环境与结果 |
| --- | --- |
| 数据库迁移 | 本机隔离 PostgreSQL 18，127.0.0.1:55435/phoskywiki_test；0016 应用成功 |
| TDD | 写入并发限制、容量预留测试先确认失败，再完成实现；按 HTTP route 边界测试真实 PostgreSQL 与外部对象存储替身 |
| 全量 Vitest | 42 个文件通过、290 个测试通过；当时尚无 R2 凭据，唯一 R2 契约跳过，随后单独补验成功 |
| 新增限制/清理 | 跨路由同账号原子计数、伪造身份/转发头、不同账号隔离、窗口恢复、无效配置、容量与待完成数拒绝、完成幂等；真实子进程测试错误目标拒绝、dry-run、过期清理、重复/迟到对象清理及永久私图保留均通过 |
| 浏览器回归 | Chromium + 本机 Next 开发服务器，3105 端口；auth、production、review-behavior、write-limits 共 9 项通过，涵盖注册/登录、词条向导、导入、两票审核及并发限额 |
| 真实 R2 契约 | 1 项通过，11.28 秒；预签名 PUT、签名 Content-Type、HEAD、错误 ETag 条件拒绝、复制冻结、重放不改原图、重复清理、拒绝删除永久 key、永久图字节保留 |
| 真实 R2 浏览器 | 1 项通过，41.4 秒含启动；真实文件输入/CORS 上传、私图匿名拒绝、第一票仍私有、第二票公开、暂存 PUT 重放、完成幂等、正文移除图片后的历史修订引用、实际清理进程与清理后永久图片字节读取 |
| 静态与构建 | `pnpm typecheck`、`pnpm lint`、`pnpm build` 通过；新增真实 R2 测试后再次 typecheck 与定向 eslint 通过 |
| 清理证据 | 两条真实 R2 测试 finally 清理自身对象，随后 S3 列表确认测试桶剩余 0 个对象，见 [脱敏回执](d05-r2-evidence.json) |

## Cloudflare 资源

通过 Cloudflare MCP 创建 APAC/Standard 独立桶 `phoskywiki-d05-test`，配置仅允许 localhost:3000/3105 的 PUT/GET/HEAD CORS。MCP 令牌管理接口返回 9109；用户随后在控制台创建只允许此桶对象读写的令牌。凭据从成功页面直接保存到维护者本机受保护目录，未写入 Git、命令参数或测试报告；Windows ACL 确认仅当前用户与 SYSTEM 可访问。保留测试桶和该受限凭据以便复验。

没有使用生产图片桶或备份桶存放测试对象，没有更改生产站点配置。本地真实浏览器/R2 验收不等于 Vultr 生产网络验收。

## Code review

Standards：独立审查未发现明确违反仓库规范的问题；一个非阻断建议为未来复用运维入口的数据库目标验证逻辑，本任务保留既有显式检查以限制改动范围。

Spec：初审唯一未完成项是真实 R2 证据，现已补齐。专用 R2 测试审查指出审核响应没有 pageId、清理可能空操作仍通过，两项均已修正并复核，随后真实执行通过。

## D07 仍需执行

生产调度启用及告警接入；真实 Cloudflare → Caddy → Next.js 的认证 IP 信任链、缓存与伪造转发头复验；Vultr 2 GB 资源与大陆网络组合验收。应用完成校验 10 MiB 和声明字节配额均不是供应商接收流量或账单硬上限，实际费用仍需 R2 指标与账单告警。
