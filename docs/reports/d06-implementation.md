# D06 实现与验收记录

日期：2026-09-10；任务：GitHub #40；起始提交：`6d24692ff95b8bca4af149fc1fa74da2284020cd`。本次在当前分支实现，承接工作区已有的备份回执、保留清理和 GitHub 告警草稿；其他 D01/D04 修改及已有删除保持原状。提交信息中的 D06 #40 可定位最终代码提交。

## 已实现

- 增量同步失败持久化，管理员私有状态接口和受目标校验保护的状态命令；保留轻量 `/healthz`。
- 所有全量重建入口与增量同步共享数据库锁；重复重建拒绝执行，晚于重建开始的失败不能被旧快照清除。重建被杀后仍显示待恢复状态。
- 每六小时搜索校对，384 MB/0.5 CPU 预算，20 分钟超时和专属容器清理；与十二小时备份共用宿主锁。备份创建并完整验证后才更新恢复点回执，超时有容器清理兜底。
- 十四天/四周恢复点保留，保守保留共享图片；扫描备份前缀字节数，外部容量阈值默认 10 GiB，可配置。
- 外部站点/数据库健康/登录/TLS 检查，备份失败与年龄、任务停止、搜索漂移与校对过期、磁盘/内存/重启/日志轮转和备份容量告警。GitHub 状态变化通知去重，不输出原始服务异常或秘密。
- [操作手册](../d06-maintenance.md)：部署顺序、搜索排查、任务安装、邮箱接入、费用/域名/MFA/轮换/缺席检查表和故障时间记录。

## 本地验证

| 验证 | 环境与结果 |
| --- | --- |
| TDD 首次失败 | 新状态路由尚不存在时搜索测试失败；新增监控字段/阈值测试先失败，再实现后通过 |
| 搜索集成 | 真实 PostgreSQL 18.3（独立 phoskywiki_test、55435）和 Meilisearch 1.15（57735）；13 项通过 |
| 实际增量失败 | 对专用 d06-reconciliation-test 索引用错误测试密钥发真实请求；词条/视角/讨论成功发布，搜索无新文档，状态降级；校对后公开搜索恢复 |
| 可见性与目标保护 | 待审内容不入索引；删除词条使其视角和讨论不可见；实际 CLI 拒绝错误搜索目标与重叠锁，成功重建输出数量和峰值 RSS |
| 并发回归 | 替身索引延迟重建，真实 PG 中新编辑提交后等待 15 秒锁超时；旧重建完成后仍显示降级，下一次重建恢复新内容 |
| 归并兼容性 | 原测试禁用搜索仍期待重建成功，修正为真实独立 consolidation-test 索引；归并后从公开搜索校验结果 |
| 全量 Vitest | 最后一轮开始于本地时间 15:30:06；42 文件通过、1 文件跳过；292 测试通过、1 跳过，160.47 秒 |
| 最终命令退出回归 | 退出清理修复后，search/bootstrap/recovery-command 三个文件 21 项通过（15:36:54 开始，38.34 秒）；随后类型检查与 lint 通过 |
| 运维测试 | `node --test scripts/backup-retention.test.mjs scripts/monitor.test.mjs`：10 项通过 |
| 类型与静态检查 | `pnpm typecheck`、`pnpm lint` 通过；Python 两个 runner/采集器语法解析通过；git diff whitespace 检查通过 |
| 生产构建 | `pnpm build` 成功，包括新管理员状态路由；没有部署到生产 |
| 本地 HTTP 冒烟 | 维护者按提供的本地生产模式命令启动服务并提供 curl 原始输出；2026-09-10 07:42:42–43 UTC（北京时间 15:42:42–43），localhost:31306 的 `/healthz` 返回 200 且 postgres=up，`/login` 返回 200 HTML，未登录访问 `/api/admin/search/status` 返回 401 和“管理员操作需要登录”，三项符合预期 |
| 运维配置 | Compose 实际解析 ops 为 0.50 CPU、402653184 字节内存；生产基础镜像中 GNU timeout 可用。未将配置解析当作生产资源实测 |

跳过项为真实 R2 对象存储契约测试：本轮未提供独立 R2 契约配置，因此不计为通过。没有为测试访问生产数据或生产搜索索引。采样在容器内用 timeout 限制十秒（两秒后强杀），防止外部 docker exec 超时后留下悬挂采样进程。补测发现 Windows Node 强制退出时的句柄断言，已改为 CLI 关闭数据库连接池后自然退出。

本地 HTTP 证据由维护者提供，本次核对响应原文，并非代理重新执行。此前启动服务被执行层以 `blocked by policy` 拒绝的缺项已由这次手动检查补齐；该证据不包含登录后管理员状态、浏览器交互或生产邮件验收。

## Standards

初审 2 项：长重建可能擦除更新的失败标记（P1）；备份超时可能留下容器使资源锁失效（P2）。已分别以 PG 时间比较的原子更新和专属容器清理修复，回归测试覆盖前者。规范审查复核无未解决问题。

## Spec

初审 2 项：备份超时清理（P2，与规范轴重复）；缺少备份容量阈值（P2）。已补充完整前缀字节采样、回执传递、可配置阈值及测试。规格审查复核无未解决的代码阻断项。

## 后续生产演练

维护者随后授权生产部署和真实邮件演练。通知通道、备份 timer 停止、备份服务失败和应用停站四轮邮件均已由维护者确认收到；受控发布已部署合格应用 e52832c，迁移、搜索校对、备份及新版监控通过，维护中断 15 秒。见 [生产演练记录](d06-live-operations.md)。下面的未完成项按该记录更新；本地实现阶段与后续生产证据分别保留。

## 尚未完成的实际验收

本票不能标为全部验收完成，保留 #40 的未验收状态：

- 新版生产镜像、数据库迁移、搜索 timer 和监控采集器已安装启用；生产强制超时等后续故障矩阵仍待验收。
- 已授权并确认通知、应用停站、备份失败和停止 timer 的真实邮件；仍未完成整机停止、过期与容量等阈值的真实邮件收件验证。GitHub Issue 成功或替身阈值测试都不能代替实际邮件。
- 已重跑真实 R2 保留清理并上传新 backupBytes 回执、触发容量阈值告警；仍未配置用户批准的费用预算/供应商提醒。默认字节阈值不是费用封顶。
- 未在生产验证 systemd 超时后的容器清理、日志实际轮转、源站证书续期、2 GB 主机资源峰值。
- GitHub 调度本身可能延迟/停用，当前方案不能监督自身整体失效；立即邮件和固定发现上界未得到保证。
- D07 仍需暂存清理的生产调度/失败信号接入、独立恢复/离线副本、实际故障发现—邮件—人工响应—公网恢复分段耗时。

初始实现阶段未推送、发布、采购资源或发送通知。后续已按授权完成新版应用部署及真实告警邮件演练；剩余验收边界见生产演练记录。
