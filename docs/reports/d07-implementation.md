# D07 实施与真实环境验收

日期：2026-09-10。工单 [#41](https://github.com/raime7/PhoskyWiki/issues/41)。起始仓库提交 `b083cb1626919084f5b6ef79799addfe6510c860`。操作手册见 [D07 运维](../d07-operations.md)。**已部署并补验主要技术链路，但不宣布全部试运行验收完成，不关闭工单。**

## 运行版本与本次变更

应用保持 D04 合格产物 `e52832c5609a70e89043b555e2a695f39a15f195`，镜像 `sha256:7277ea092bc3d44f4c30737da8e0acc6ae8562898ba06cc0fbdb866b860d7318`。既有生产发布与 CI 资格见 [D06 生产记录](d06-live-operations.md)。本次没有重新初始化、导入本地内容或切换应用版本。

新增代理镜像 Caddy 2.11.2 + Cloudflare DNS 0.2.4，由固定基础镜像本机构建；代理基础设施产物未冒充 D04 应用资格收据。归档 SHA-256 `be59bf789bbcfbeb41330e3ec1d666cc7e745edc6fd3c21a095f60e63a0ec212`，两端一致。config digest `sha256:35b76cfd5847b641ee327e08266a775f78ef4b3e324ae13133f5b2c37b1ba83b`，平台 index ID `sha256:b0bff5130679a8e58ec6b87f1337643b82029aa9ce1c8bfcd0fd27df376a3d50`。首次按 config ID 比对被平台差异拒绝并恢复旧配置，核对同一归档及 manifest 后再次部署成功；未跳过校验。成功操作共 14.48 秒（包含镜像加载与检查），不等于逐秒测出的停机时长。

## 真实环境结果

| 范围 | 实际证据与边界 |
| --- | --- |
| DNS / HTTPS | apex 保持 Cloudflare 代理 A 与 Full (strict)；新增 www 代理 CNAME，308 保留路径与查询跳转主域。初始 www 无解析、源站直连 200，现已修复 |
| 证书 | www 于 10:01 UTC 通过真实 Let's Encrypt DNS-01 签发。独立复制临时验收域证书存储、提前进入续期窗口，真实 `tls.renew` 成功；原站证书存储未改。续期证据（本机原始证据，不随仓库分发：`d07-renewal.json`） |
| 令牌 | 账号级管理令牌只在本机使用，生成仅本站 zone 的 DNS Write + Zone Read。随后同权限轮换，验证新令牌 TXT 创建/删除、新令牌 DNS 请求 200、旧令牌撤销后 401。轮换证据（本机原始证据，不随仓库分发：`d07-dns-rotation.json`） |
| 源站 | Caddy peer 检查使直连返回 403；之后 nftables 在 Docker DNAT 前过滤默认网卡，外部 TCP 443 超时。内部 3000/5432/7700/8080/2019 均未连通。规则重复应用成功，设置开机启用；本轮未重启主机验证开机路径 |
| IPv6 | 清单和防火墙包含 7 段 Cloudflare IPv6；源站当前未配置全局 IPv6。没有把配置存在当作外部 IPv6 链路实测成功 |
| 来源与限流 | 伪造 X-Forwarded-For/X-Real-IP 的四次登录为 401/401/401/429；伪造 CF-Connecting-IP 被边缘直接拒绝为 403。网络证据（本机原始证据，不随仓库分发：`d07-network.json`） |
| 多来源 | 同一限流时段内，本机首次四次 401/401/401/429；独立 Vultr 出口返回 401；随后本机仍为 429，证明未共用一个代理桶。双客户端证据（本机原始证据，不随仓库分发：`d07-ip-clients.json`） |
| 缓存 | Cloudflare 明确绕过非静态资源缓存。主站健康、登录、会话、管理员状态采样均 DYNAMIC、无 Age；隔离环境两管理员/游客浏览器会话不串号 |
| SSH | 保留已验证独立运维密钥与 forced-command 发布/监控身份；新增 root 仅公钥、禁用密码和键盘交互认证，并以新 SSH 连接复验。供应商控制台恢复未在本轮实测 |
| 图片与账号 | 同一 Vultr 上的临时域 `d07-check.phoskywiki.org` 使用合格应用镜像、独立 PG/搜索/测试桶，真实浏览器注册、上传、私图、两票受理、冻结重放、导航、密码重设及旧会话撤销通过。首次（本机原始证据，不随仓库分发：`d07-browser-initial.json`）、负载期间复验（本机原始证据，不随仓库分发：`d07-browser.json`） |
| 暂存清理 | 生产每小时 timer active，首次 service success。隔离环境标记自有图片夹具过期后，实际 CLI 删除 3 个 staging 对象，保留 3 张永久图片；第二次处理 0 条；之后公开/私有图片浏览器字节与权限仍通过。清理行为（本机原始证据，不随仓库分发：`d07-staging-test.json`） |
| 完整恢复 | 在测试桶备份数据库与 3 张永久图片，完整校验并导出加密文件；恢复到第二个空测试库及独立对象前缀，重建搜索。操作约 12.74 秒。进程证据（本机原始证据，不随仓库分发：`d07-restore.json`） |
| 恢复后行为 | 原两管理员凭据、编者重设后的密码仍可登录，公开图片字节一致、私图游客拒绝/编者可读、搜索含原内容；清理后复验成功。浏览器证据（本机原始证据，不随仓库分发：`d07-recovery-browser.json`） |
| 组合资源 | 120 秒，四个 HTTP worker 与浏览器登录/上传/受理并行，期间真实生产备份和隔离索引重建成功；870 请求、0 失败、宿主内存峰值 1,287,471,104 字节（约 1.20 GiB）、swap 峰值 21,614,592 字节、无容器 OOM。资源证据（本机原始证据，不随仓库分发：`d07-load.json`） |
| 清理 | 删除并逐个 HEAD 确認本次 18 个测试对象不存在；恢复测试桶原 CORS，删除临时 DNS，撤除临时 Caddy 路由；测试容器、网络、凭据及解密数据清理。对象回执（本机原始证据，不随仓库分发：`d07-cleanup.json`） |

生产库保持原内容；新增词条、编者与图片仅存在于已删除的独立验收环境。资源采样每 200 ms，只覆盖几篇测试内容和 68 字节 PNG，不能证明大库、大图片或高并发容量。恢复测试故意遵循维护者“同机隔离、不另购服务器”的范围调整，未测试整机丢失。

## 全部规格矩阵的状态

| 必过项 | 状态 |
| --- | --- |
| 空库初始化、重复/并发、错误目标、seed 防护 | 沿用 D01 真实容器与历史生产初始化证据；本次未重复初始化生产 |
| 邀请边界、并发、过期/撤销、伪造角色 | 沿用 D02 集成/浏览器证据；本次真实网络复验有效邀请、普通角色与重放 |
| 登录、刷新、重设、会话撤销、双人受理 | 同机隔离真实域浏览器通过；应用与生产为同一固定镜像 |
| R2 浏览器 CORS、冻结、私图、受理公开、清理、恢复 | 本次真实 R2 + Vultr + Cloudflare 通过；测试桶与正式数据隔离 |
| 合格产物与手动生产发布 | D06 已通过 D04 门禁发布本次运行版本；本次只更新代理/运维配置 |
| 发布失败切回、迁移失败与新增内容保留 | D04 隔离容器契约已有证据；本轮未在 Vultr 重跑完整发布故障矩阵，不能标为本轮通过 |
| 真实故障邮件 | D06 已确认五轮维护者收件；新增 staging 信号测试通过，分支上的真实外部监控运行成功，合入 main 后进入原定时调度。Karl 新接收渠道见下方状态 |
| 大陆整站及 R2 | D01 六城市 HTTP 拨测为历史证据；本次桌面 Chromium 完整链路可用，但没有重新核实桌面网络运营商/出口，不能当作完整大陆线路矩阵 |
| RPO / RTO | 本次组合负载生成新的完整生产备份；同机恢复操作成功。整机丢失、发现—收件—响应—机器准备—公网验证的 24 小时完整 RTO 未实测；替代主机演练已由维护者取消 |
| 日常运维与费用 | 手册已给出日/周/月操作，用户指定 US$20/月提醒额度及联系 Karl Aurora；供应商总账单提醒、接收渠道和代班落实仍需补齐 |
| 独立副本、账号恢复、全套凭据轮换 | DNS 轮换与续期实测通过，已有 D01 测试令牌撤销证据；未轮换所有生产凭据。用户确认没有第二设备，资料仅存本人电脑；尚未具备设备级独立副本 |

## 验证与审查

- [真实外部监控运行](https://github.com/raime7/PhoskyWiki/actions/runs/34466995848)成功，使用新增 staging 判定和受限 SSH 身份；最终主机状态（本机原始证据，不随仓库分发：`d07-final-status.json`）显示四个容器健康、备份及清理成功、搜索可用。此运行来自功能分支，定时调度仍须以合入 main 为准。
- 类型检查、ESLint 通过；完整 Vitest 为 292 通过、1 条真实 R2 契约因未注入专用凭据而未执行。本次另行实际浏览器/R2 验收不将该 skip 改写为通过。
- 运维 Node 测试 24 项通过；新增 staging 判定先观察失败再实现。真实 Caddy 容器测试覆盖来源拒绝、头重写、重定向及回环入口边界。
- Standards 独立审查未发现规范违反；发现手册重建命令缺标志和测试辅助容器超时清理问题，均已修复并重跑代理测试。
- Spec 独立审查未发现代码错误，指出交付手册缺失，现已补齐。上述真实环境未完成项继续保留，不以代码审查通过代替总体验收。

## 需要维护者完成的安排

维护者明确：US$20/月为费用提醒额度；超过 12 小时无法响应时联系 Karl Aurora（管理员 QQ 邮箱，地址不进入公开报告）；恢复资料仅保存在维护者电脑，没有第二设备。新增 DNS 凭据已保存于受保护目录，仍需纳入加密密码库更新与第二副本计划。

Karl 测试邮件尚未发送；最初 Gmail 返回重新授权要求，维护者重连后读取和发送接口均返回 `Unknown tool`，当前连接器无法完成投递。无需重复提交凭据。恢复接口后仍需测试发送并确认收件及代班安排；发送成功也不等于对方接受代班。现有 GitHub 告警继续保留，不能误报已经自动抄送 Karl。
