# D02 implementation plan

Spec: GitHub #36, parent #34. Review baseline: `8af127426a6f8ee78feefbb36fbf234059312e7b`.
The approved seams are HTTP/browser behavior with real PostgreSQL and maintenance subprocesses.

- [x] Close built-in registration; add purpose-separated hashed single-use grants with transactional registration and password reset. Check bypass, rollback, expiry, revocation, replay, concurrent exchange and role injection through HTTP.
- [x] Add administrator grant issue/list/revoke API and UI, invitation registration and reset forms. Use URL fragments, no-store and no-referrer; never return hashes/passwords or log request bodies.
- [x] Add targeted administrator emergency recovery to the existing production command. Validate database identity, explicit account ID/email and protected credential file before mutation. Check with subprocesses and login/session behavior.
- [x] Adapt isolated authentication fixtures; browser-test grants and registration/login/reload/logout/reset plus existing two-admin submission acceptance.
- [x] Run typecheck during slices, focused tests after each slice, then lint, full unit/integration suite and browser suite. Review standards/spec independently, fix findings, commit only D02 files on current branch.

Architecture: `src/lib/access-grants.ts` owns token lifecycle and database transactions; HTTP handlers enforce origin and administrator permissions; Better Auth retains login/session handling with registration disabled in every environment. Existing quorum snapshot logic stays authoritative. Defaults: invitation 7 days, reset 1 hour, configurable in seconds. Recovery is manual identity checking, not email verification. No automatic mail or production seed.
