# D04 implementation plan

Source: GitHub #38, parent #34. Baseline: `6d24692ff95b8bca4af149fc1fa74da2284020cd`.
The approved specification defines browser/HTTP and operations subprocess/container seams.
Keep unrelated working changes; commit D04 on the current branch.

1. Add release qualification CLI: exact main push, CI workflow, successful required jobs, same attempt receipt, immutable image and revision. Exercise accepted and rejected evidence through its public boundary.
2. Build one application image in CI; run the existing container acceptance and full Playwright suite against that image. Publish the tested image after lint/typecheck/unit/integration/browser checks, recording image ID, registry digest, source and run attempt. PR jobs receive no deployment secrets. Add Dependabot.
3. Add serialized host release command and manual Actions workflow. Pin SSH identity and source; verify target, resources and old image; stop writes, create and verify D03 recovery point, migrate and switch. Persist phase records; compatible fallback never restores data. Interrupted/failed migrations require operator recovery.
4. Exercise real containers and operations processes for success, wrong target, concurrency, missing backup, unhealthy app, migration failure and content preservation. Run focused tests and typecheck during implementation; full suite once at the end.
5. Configure/check actual main branch protection where permissions permit. Record baseline failures and unexecuted external acceptance honestly. Write release/update/cancel/recovery runbook and evidence report.
6. Review the D04 diff against the baseline with independent Standards and Spec agents per code-review skill, fix findings, commit only D04 files/hunks.
